# Шаблон секретов. Скопируй в .env.local и заполни.
# .env.local в .gitignore — секреты не коммитятся.
#
# ── Пример секции ───────────────────────────────────────────────────────────
# SOME_API_KEY=            # откуда взять: https://example.com/dashboard

# Передача следующей сессии

Что нужно знать тому, кто продолжит. Обновляется в конце сессии вместе с `PROGRESS.md`.

## Где мы

Фазы 1 и 2 завершены: 12 из 12 фич `passing`, каждая с одобренным ревью, 151 тест. Сквозной путь работает на живом MCP, снапшот направлений построен.

Ветка: `feature/discovery-swipe`. Активная фаза по реестру — третья.

## Что важно не потерять

1. **Discovery в MCP отсутствует** — это причина существования проекта. Все `search_*` требуют явный `destination`.
2. **Первый экран строится на снапшоте, а не на живом поиске.** Замер: холодное направление отвечает 10,8 с, живой сквозной запрос занял 14,4 с. Бюджет в 1,5 секунды (AC7) достижим только предзаготовленными данными.
3. **Транспорт и отели по-разному сообщают о неразрешимом городе** — см. `docs/surprises.md`. Транспорт отвечает успехом с причинами, отели флагом ошибки и не-JSON текстом.
4. **Границы слоёв проверяются машиной** и ловят алиасы, импорт типов и транзитивные реэкспорты. Обойти их не выйдет — нужные типы передаются параметрами.
5. **Клиенту доверять нельзя** (фаза 3): состояние подписывается, модель пересчитывается из журнала реакций.

## Особенности инструментов

- **Codex** запускать с `< /dev/null`, иначе ждёт ввода со stdin и висит бесконечно. Флаг сетевого доступа не нужен: пакеты и фикстуры готовит оркестратор, тесты офлайн.
- **Qwen** только с `--model qwen3.8-max`. Порции по 30 строк кода: на 2 КБ уже случаются таймауты. Примерно треть замечаний — ложные срабатывания из-за узкого контекста, проверять по коду до исправления.
- **Правки от Codex приходят без тестов**, если тесты не потребованы явно в задании на исправление.

## Что дальше

Фаза 3 (Обучение и интерфейс), до 18 августа. Фичи F13–F17 заведены.

Порядок: подписанное состояние сессии → лента свайпа → ранжировщик с реакциями → базовая линия → объяснения.

**Контрольная дата 15 августа**: отчёт `docs/ranking-baseline.md` должен быть получен до неё, иначе решение о точке отсечения по модели принимать не на чем.

**Главный незакрытый пункт с фазы 2**: маршрут отдаёт JSON целиком, поэтому AC7 (первая карточка за 1,5 с) до пользователя не доведён. Снапшот отдаёт данные за 2,2 мс — узкое место в доставке, а не в данных.

## Деплой (решено 2026-08-06)

VPS `185.79.138.118`, Traefik уже настроен, схема — по шаблону `/opt/apps/_template-compose`.
Домен `tutu-swipe.185.79.138.118.nip.io`, образ собирается локально (на сервере мало памяти).
Нужен маршрут `/health` и standalone-сборка. Публикация — только с разрешения владельца.

## Долг и отложенное

- Отчёт о находках по API Туту для команды сервиса — собрать 18 августа из `docs/surprises.md` (5 записей).
- Прочее — в `docs/tech-debt-tracker.md`.

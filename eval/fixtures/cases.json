{
  "threshold": 0.85,
  "cases": [
    {
      "id": "C01",
      "tag": "{{категория — по ней строится heatmap: greeting, pricing, edge-case}}",
      "input": { "text": "{{вход, который получит LLM}}" },
      "expect": { "contains": "{{фрагмент, который обязан быть в ответе}}" },
      "note": "{{чем этот кейс сложен — зачем он в наборе}}"
    }
  ]
}
